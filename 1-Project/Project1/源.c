#include <stdio.h>
//写代码
//1.写出主函数（main函数）
//C语言的代码都是从主函数（main）执行。main函数是C语言的入口，
int main()//圆括号代表这是一个函数，main是函数名，下面的大括号是函数体，也就是函数的范围。int是函数的返回类型，int是整型，return0就是返回0，0就是整型
{
	printf("黄荣彬\n");//printf是一个库函数，用于在屏幕是打印信息的
	return 0;
}

//编译+链接+运行代码。快捷键=Ctrl+F5